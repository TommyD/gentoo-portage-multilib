#!/bin/bash
# Copyright 1999-2003 Gentoo Technologies, Inc.
# Distributed under the terms of the GNU General Public License v2
# $Header$

if [ -n "${DEBUGBUILD}" ] ; then
	exit 0
fi

if [ ! -z "${CBUILD}" ] && [ "${CBUILD}" != "${CHOST}" ]; then
	STRIP=${CHOST}-strip
else:
	STRIP=strip
fi

for x in "$@"; do # "$@" quotes each element... Plays nice with spaces.
	if [ -d "${D}${x}" ]; then
		# We only want files. So make a pass for each directory and call again.
		find "${D}${x}" -type f \( -perm +0111 -or -regex '\.so$|\.so\.' \) -print0 |
				sed -r 's:(\d000?)${D}:\1:g' | xargs -0 -r -n500 prepstrip
	else
		f=$(file "${x}")
		if [ -z "${f/*SB executable*/}" ]; then
			echo "${x}"
			${STRIP} "${x}"
		fi
		if [ -z "${f/*SB shared object*/}" ]; then
			echo "${x}"
			${STRIP} --strip-debug "${x}"
		fi
	fi
done
